# Fichier de configuration des routes
controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# Route pour la page de logout
app_logout:
    path: /logout
